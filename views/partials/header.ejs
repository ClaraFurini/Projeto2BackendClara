<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Empresa Fictícia</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: #fff;
            text-decoration: none;
            display: flex;
            align-items: center;
        }
        .logo-icon {
            color: #9c27b0;
            margin-right: 10px;
        }
        nav {
            display: flex;
            gap: 20px;
        }
        nav a:hover {
            color: #ba68c8;
            transition: color 0.3s;
        }
    </style>
</head>
<body>
    <header>
        <a href="/" class="logo">
            <span class="logo-icon">⚡</span>
            <span>Empresa Fictícia</span>
        </a>
        
        <nav>
            <% if (user) { %>
                <a href="/admin/dashboard">Dashboard</a>
                <a href="/auth/logout">Sair</a>
            <% } else { %>
                <a href="/">Home</a>
                <a href="/auth/login">Login</a>
            <% } %>
        </nav>
    </header>

    <main>